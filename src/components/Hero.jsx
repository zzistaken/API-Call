function Hero() {
  return (
    <section className="hero is-link">
      <div className="hero-body">
        <p className="title">API Call</p>
        <p className="subtitle">
          An app that allows us to search for images on Unsplash with the help
          of Axios. Built with React + Vite, styled with Bulma CSS.
        </p>
      </div>
    </section>
  );
}

export default Hero;
